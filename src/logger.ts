
export function info(sub : string, message : any)
{
    console.info(`{Master} [${sub}] ${message}`)
}

export function warn(sub : string, message : any)
{
    console.warn(`{Master} [${sub}] ${message}`)
}